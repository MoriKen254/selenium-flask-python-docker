# Build-time Proxy Configuration
# Copy this file to .env.build and configure for your environment
# These values are used during Docker image builds

# HTTP/HTTPS Proxy (leave empty if no proxy needed)
HTTP_PROXY=http://your.proxy.co.jp:port
HTTPS_PROXY=http://your.proxy.co.jp:port

# No Proxy - addresses that should bypass the proxy
NO_PROXY=localhost,127.0.0.1,frontend,backend,postgres
